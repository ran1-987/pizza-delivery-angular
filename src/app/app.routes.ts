import { Routes } from '@angular/router';
import { PizzaViewComponent } from './components/pizza-view/pizza-view.component';

export const routes: Routes = [
    {path:'',component:PizzaViewComponent}
];
